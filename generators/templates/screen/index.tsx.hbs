{{#if useProps}}
import React from 'react'
import { Screen, Text } from '@/components'
import type { {{pascalCase name}}ScreenProps } from './{{pascalCase name}}.types'

export const {{pascalCase name}}Screen = ({ }: Readonly<{{pascalCase name}}ScreenProps>) => {
    return (
            <Screen><Text>{{pascalCase name}}</Text></Screen>
    )
}


{{else}}
import React from 'react'
import { Screen, Text } from '@/components'

export const {{pascalCase name}}Screen = () => {
    return (
            <Screen><Text>{{pascalCase name}}</Text></Screen>
    )
}


{{/if}}
